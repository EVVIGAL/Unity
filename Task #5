using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Alarm : MonoBehaviour
{
    [SerializeField] private AudioSource _audioSource;
    private float _minVolume = 0.0f;
    private float _maxVolume = 1.0f;
    private float _increaseRate = 0.2f;
    private bool _audioIsStoping = false;


    private void Start()
    {
        _audioSource.volume = _minVolume;
    }

    private void Update()
    {
        if (_audioSource.isPlaying && _audioIsStoping == false)
        {
            _audioSource.volume += Mathf.MoveTowards(_minVolume, _maxVolume, _increaseRate * Time.deltaTime);
        }
        
        if (_audioIsStoping == true)
        {
            _audioSource.volume -= Mathf.MoveTowards(_minVolume, _maxVolume, _increaseRate * Time.deltaTime);

            if (_audioSource.volume <= _minVolume)
            {
                _audioSource.Stop();
                _audioIsStoping = false;
            }
        }
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.TryGetComponent<Player>(out Player player))
        {
            _audioIsStoping = false;
            _audioSource.Play();
        }
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        if (collision.TryGetComponent<Player>(out Player player))
        {
            _audioIsStoping = true;
        }
    }
}
